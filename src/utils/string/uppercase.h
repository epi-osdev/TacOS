#ifndef __EOS_UTILS_STRING_UPPERCASE_H__
    #define __EOS_UTILS_STRING_UPPERCASE_H__

/**
* @brief This function changes all the characters of a 
* string to uppercase if they are not already uppercase
* @param str the string you want to capitalize
*/
void uppercase(char *str);

#endif