#ifndef __EOS_UTILS_STRING_ISLOWER_H__
    #define __EOS_UTILS_STRING_ISLOWER_H__

/**
* @brief This function returns 1 if the given character
* is lowercase, otherwise it returns 0
* @param c The character to check
* @return: 1 if the given character
* is lowercase, else 0
*/
int islower(char c);

#endif